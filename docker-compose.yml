services:
  infracalendar:
    build: .
    image: infracalendar:latest
    network_mode: "host" # allows container to reach host localhost:8002 on Linux
    environment:
      - AUTH_TOKEN=secret
      - OLLAMA_BASE_URL=http://ollama:11434
